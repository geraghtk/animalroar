; PlatformIO project configuration
; https://docs.platformio.org/page/projectconf.html

[env:nicla_voice]
platform = nordicnrf52
board = nicla_voice
framework = arduino

monitor_speed = 115200
upload_port = COM4
monitor_port = COM4

; Library dependencies — add as the project grows.
; Pin to a major version (e.g. ^1.2.0) so builds stay reproducible.
lib_deps =
